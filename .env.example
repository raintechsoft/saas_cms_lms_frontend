# Local development: call the backend directly.
VITE_API_URL=http://127.0.0.1:4000/api/v1

# Optional same-origin proxy style (requires Vite proxy in vite.config.ts):
# VITE_API_URL=/api/v1

# Optional — must match backend GOOGLE_CLIENT_ID for the Google Sign-In button
# VITE_GOOGLE_CLIENT_ID="xxxx.apps.googleusercontent.com"

# Optional — backend PUSH_VAPID_PUBLIC_KEY
# VITE_PUSH_VAPID_PUBLIC_KEY="B..."
