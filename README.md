# SaaS CMS LMS — Frontend

React + Vite + Tailwind management UI (campus, portal, Super Admin).

## Setup

```powershell
npm install
Copy-Item .env.example .env
# set VITE_API_URL to backend, e.g. http://localhost:4000/api/v1
npm run dev
```

App: `http://localhost:5173`

## Scripts

| Command | Description |
|---------|-------------|
| `npm run dev` | Vite dev server |
| `npm run build` | Production build |
| `npm run preview` | Preview build |
| `npm run test` | Vitest |

## Related repo

Backend: `saas_cms_lms_backend`
